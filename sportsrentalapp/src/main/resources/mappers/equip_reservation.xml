<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- name space는 BookRepo의 fully qualified name으로 설정한다. -->
<mapper namespace="com.ssafy.sports.model.dao.EquipReservationDao">
    <select id="selectEquipResByUid" parameterType="String" resultType="EquipReservation">
        select res_id, user_id, er.equip_id, res_start_time from equip_reservation er
        inner join equip e
        on er.equip_id = e.equip_id
        where er.user_id = #{userId};
    </select>

    <select id="selectEquipResByRid" parameterType="Integer" resultType="EquipReservation">
        select * from equip_reservation er inner join equip e
        on er.equip_id = e.equip_id
        where er.res_id = #{resId};
    </select>

</mapper>